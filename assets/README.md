# assets/

This folder contains static assets used by Webrella Studio, including images, icons, fonts, and other downloadable files.

Suggested structure:

- `assets/images/` — production images and optimized graphics
- `assets/icons/` — SVG icons
- `assets/fonts/` — self-hosted fonts with appropriate licensing information

Large generated or build-only files should stay out of the repository when possible.
